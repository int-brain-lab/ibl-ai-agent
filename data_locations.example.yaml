# Copy this file to data_locations.local.yaml and fill in paths for this computer.
# data_locations.local.yaml is gitignored because these paths are user-local.

datasets:
  bwm_ephys:
    # Directory containing version folders such as 1.1.0/, or a specific
    # dataset directory that contains schema.yaml.
    root:
    preferred_version: latest

  bwm_behavior:
    # Directory containing version folders such as 1.1.0/, or a specific
    # dataset directory that contains schema.yaml.
    root:
    preferred_version: latest

  bwm_lfp:
    # Directory containing lf_compressed_all_bwm.h5 (~14 GB).
    # Download with: uv run python scripts/download_datasets.py --lfp
    root:

one_cache:
  root:
